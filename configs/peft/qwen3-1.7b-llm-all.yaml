type: lora

# Train LoRA on every Qwen3-1.7B transformer layer.
# Pair with engine.trainability: llm=lora, everything else frozen.

llm_lora_config:
  r: 4
  lora_alpha: 8
  lora_dropout: 0.05
  bias: "none"
  target_modules:
    - "q_proj"
    - "v_proj"
